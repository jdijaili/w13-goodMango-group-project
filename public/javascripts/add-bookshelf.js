window.addEventListener("DOMContentLoaded", () => {

    const addBookshelfBtn = document.querySelectorAll(".add-button");
    addBookshelfBtn.addEventListener("click", e => {
        // show the "Add a shelf" text as a p ele
        // show the form for name
        // show the button for add
    })

});
